export function description() {
  return "Call the two live x402 services on fretchen.eu from TypeScript — the exact scheme for one-shot payments, batch-settlement channels for many small calls. No accounts, no API keys.";
}
