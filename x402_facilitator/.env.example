# Scaleway Configuration (required for deployment)
SCW_ACCESS_KEY=SCWXXXXXXXXXXXXXXXXX
SCW_SECRET_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
SCW_DEFAULT_ORGANIZATION_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
SCW_DEFAULT_PROJECT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Facilitator Configuration
# IMPORTANT: Set these as Scaleway Secrets, not in .env for production!
FACILITATOR_WALLET_PRIVATE_KEY=0x...
FACILITATOR_WALLET_PUBLIC_KEY=0x...

# Optional: Custom RPC endpoints (defaults provided in chain_utils.js)
# Set these for production to use dedicated RPC providers (Infura, Alchemy, Ankr, etc.)
# OPTIMISM_RPC_URL=https://opt-mainnet.g.alchemy.com/v2/YOUR_API_KEY
# OPTIMISM_SEPOLIA_RPC_URL=https://opt-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Local development
NODE_ENV=test
LOG_LEVEL=info

# Agent Whitelist Configuration
# All sources are checked automatically (OR logic - authorized if ANY returns true):
# - manual: MANUAL_WHITELIST env variable (works on all networks)
# - genimg_v4: GenImNFTv4 contract (deployed addresses in chain_utils.js)
# - llmv1: LLMv1 contract (deployed addresses in chain_utils.js)
# - test_wallets: TEST_WALLETS env variable (testnet only)

# Manual Whitelist (works on all networks - Mainnet and Testnet)
# Comma-separated Ethereum addresses (case-insensitive)
# Example: MANUAL_WHITELIST=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb,0xF39Fd6e51aad88F6F4ce6aB8827279cffFb92266
MANUAL_WHITELIST=0xAAEBC1441323B8ad6Bdf6793A8428166b510239C

# Test Wallets (testnet only, comma-separated addresses)
# Only works on Sepolia testnet, ignored on mainnet
TEST_WALLETS=0x...,0x...
